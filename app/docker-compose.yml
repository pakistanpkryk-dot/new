services:
  db:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: ayurvia
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: ["ayurvia_pg:/var/lib/postgresql/data"]
volumes:
  ayurvia_pg:
